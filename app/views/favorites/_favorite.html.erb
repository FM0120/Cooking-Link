<% if user_signed_in? %>
<% if recipe.favorited_by?(current_user) %>  
   <%= link_to recipe_favorite_path(recipe.id), method: :delete, class: 'btn btn-default', remote: true do %>
       ♥<%= recipe.favorites.count %>
   <% end %>
<% else %>
      <%= link_to recipe_favorites_path(book.id), method: :post, class: 'btn btn-default', remote: true do %>
         ♡<%= recipe.favorites.count %>
      <% end %>
  <% end %>
<% end %>