<table class='table'>
  <h1>My profile</h1>
  <tr><%= attachment_image_tag(@user, :profile_image, size: "100x100", fallback: "no_image.jpg") %></tr>
    <p><%= link_to 'プロフィール編集',edit_user_path(user),class: "btn btn-outline-success" %></p>
    <p><%= link_to 'My recipeへ',users_path, class: "btn btn-outline-success" %></p>
    <tr>
      <th>name</th>
      <th><%=link_to user.name %></th>
    </tr>
      <tr>
      <th>フォロー数</th>
      <th><%= link_to user.followings.count,user_followings_path(user.id) %></th>
    </tr>
      <tr>
        <th>フォロワー数</th>
        <th><%= link_to user.followers.count, user_followers_path(user.id) %></th>
      </tr>
</table>

